{
  "name": "@itharbors/client",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.build.json && vite build",
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && node ./scripts/test.mjs"
  },
  "dependencies": {
    "@itharbors/plugin-types": "0.0.1",
    "three": "^0.176.0"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "vite": "^6.0.0",
    "vitest": "^2.0.0"
  }
}
