{
  "name": "@itharbors/server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    "./testing": {
      "types": "./dist/testing.d.ts",
      "import": "./dist/testing.js"
    }
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && vitest run"
  },
  "dependencies": {
    "@itharbors/host-security": "0.0.1",
    "@itharbors/kit-core": "0.0.1",
    "@itharbors/native-credential-vault": "0.0.1",
    "@itharbors/plugin-types": "0.0.1",
    "better-sqlite3": "^12.10.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "vitest": "^2.0.0"
  }
}
