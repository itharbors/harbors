{
  "name": "workflow",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npx tsc -b && node ./dist/build.js",
    "clean": "npx tsc -b && node ./dist/clean.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@itharbors/workflow": "^1.0.1"
  }
}
